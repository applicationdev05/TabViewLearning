name: iOS CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: macos-latest
    timeout-minutes: 180          # generous timeout for clean builds
    defaults:
      run:
        working-directory: TabViewAndRouting
    steps:
      - uses: actions/checkout@v4

      - name: Configure Git for private dependencies
        run: |
          git config --global url."https://x-access-token:${{ secrets.GH_PAT_TOKEN }}@github.com/".insteadOf "https://github.com/"

      - name: Cache SPM dependencies
        uses: actions/cache@v4
        with:
          path: |
            TabViewAndRouting/.build
            ~/Library/Developer/Xcode/DerivedData
          key: ${{ runner.os }}-spm-${{ hashFiles('TabViewAndRouting/Package.resolved') }}
          restore-keys: ${{ runner.os }}-spm-

      - name: Install Ruby gems
        run: |
          gem install bundler
          bundle install

      - name: Run tests
        timeout-minutes: 170       # tests get up to 80 min, total job 90 min
        run: bundle exec fastlane ios test
